services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: setflow
      POSTGRES_PASSWORD: setflow
      POSTGRES_DB: setflow
    ports:
      - "5432:5432"
    volumes:
      - setflow_pg:/var/lib/postgresql/data

volumes:
  setflow_pg:
